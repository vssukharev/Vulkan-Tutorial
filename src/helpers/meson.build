
helpers_source = files('files.cpp')

project_sources += helpers_source

helpers_lib = static_library(
  'helpers',
  helpers_source,
  include_directories : inc,
)

project_libraries += helpers_lib

